import { defineConfig } from "astro/config";

// Opcional: pon aquí la URL real cuando tengas dominio en Vercel (útil para SEO y sitemap)
export default defineConfig({
  site: "https://sepina-studio.vercel.app",
});
